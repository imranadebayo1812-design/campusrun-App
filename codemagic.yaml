workflows:
  ios-build:
    name: CampusRun iOS
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      groups:
        - ios
      vars:
        BUNDLE_ID: com.campusrun.app
        XCODE_WORKSPACE: ios/App/App.xcodeproj/project.xcworkspace
        XCODE_SCHEME: App
      node: "22"
      xcode: latest
    scripts:
      - name: Install dependencies
        script: npm ci

      - name: Build web app
        script: npm run build

      - name: Sync Capacitor
        script: npx cap sync ios

      - name: Set up keychain
        script: keychain initialize

      - name: Fetch signing files
        script: |
          # Rebuild valid PEM from env var (handles stripped newlines)
          KEY_B64=$(echo "$APP_STORE_CONNECT_PRIVATE_KEY" \
            | sed 's/-----BEGIN PRIVATE KEY-----//g' \
            | sed 's/-----END PRIVATE KEY-----//g' \
            | tr -d ' \n\r')
          printf "-----BEGIN PRIVATE KEY-----\n%s\n-----END PRIVATE KEY-----\n" \
            "$(echo "$KEY_B64" | fold -w 64)" > /tmp/api_key.p8
          openssl genrsa -out /tmp/cert_key.pem 2048
          app-store-connect fetch-signing-files "$BUNDLE_ID" \
            --type IOS_APP_STORE \
            --private-key @file:/tmp/api_key.p8 \
            --certificate-key @file:/tmp/cert_key.pem \
            --create

      - name: Add certificates to keychain
        script: keychain add-certificates

      - name: Set up code signing
        script: xcode-project use-profiles

      - name: Build IPA
        script: |
          xcode-project build-ipa \
            --workspace "$XCODE_WORKSPACE" \
            --scheme "$XCODE_SCHEME"

    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log

    publishing:
      app_store_connect:
        api_key: $APP_STORE_CONNECT_PRIVATE_KEY
        key_id: $APP_STORE_CONNECT_KEY_IDENTIFIER
        issuer_id: $APP_STORE_CONNECT_ISSUER_ID
        submit_to_testflight: true
      email:
        recipients:
          - imranadebayo1812@gmail.com
        notify:
          success: true
          failure: true
